<div id="comment-<%= comment.id %>">
  <div class="info">
    <h4 class="name"><%= comment.current_user.name %></h4>
  </div>
  <div class="content">
    <div class="row">
      <span class="text"><%= comment.comment_html %></span>
        <% if belongs_to_current_user?(comment) %>
        <%= link_to comment_path(comment), method: :delete, class: 'btn btn-danger btn-sm', remote: true do %>
          <% fa_icon 'trash' %>
        <% end %>
      <% end %>
    </div>
  </div>
  <br>
  <hr>
</div>